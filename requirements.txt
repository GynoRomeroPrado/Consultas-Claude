# Core PDF extraction
PyMuPDF>=1.23.0
pdfplumber>=0.10.0

# Fuzzy matching
rapidfuzz>=3.5.0

# Data manipulation
numpy>=1.24.0
pandas>=2.0.0

# Image processing (FASE 1 + FASE 2)
Pillow>=10.0.0
opencv-python>=4.8.0  # Preprocesamiento de imagen (denoising, deskewing, binarización)

# OCR (FASE 2 - Pipeline Híbrido)
paddlepaddle>=2.5.0  # CPU version (para GPU: paddlepaddle-gpu)
paddleocr>=2.7.0  # OCR robusto para PDFs rasterizados

# JSON/XML handling (built-in, but for validation)
jsonschema>=4.20.0

# Progress bars and CLI
tqdm>=4.65.0
click>=8.1.0

# Logging
colorlog>=6.7.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Type hints
typing-extensions>=4.8.0

# Optional: Visualization
matplotlib>=3.7.0
